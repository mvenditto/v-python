name: ci

on:
  workflow_dispatch:
  push:
    paths-ignore:
      - '**.md'
      - '**.sh'
      - 'Vagrantfile'
jobs:
  run:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up V version latest
        uses: nocturlab/setup-vlang-action@v1
        with:
          v-version: latest
        id: v
      - name: Install build deps
        run: |
          sudo apt update -y
          sudo apt-get install -y python3-dev python3.9
      - name: Set PY_HEX_VERSION
        run: echo PY_HEX_VERSION="$(python3 -c 'import sys; print(hex(sys.hexversion))')" >> $GITHUB_ENV
      - name: Set cflags
        run: echo CFLAGS="$(pkg-config python3 --cflags-only-I) -DPy_LIMITED_API=$PY_HEX_VERSION" >> $GITHUB_ENV
      - name: Set ldflags
        run: echo LDFLAGS="$(pkg-config python3-embed --libs)" >> $GITHUB_ENV
      - name: Run tests
        run: v -stats -showcc -d py_limited_api -cc gcc test $GITHUB_WORKSPACE/*_test.v
      - name: Create the Badge
        uses: schneegans/dynamic-badges-action@v1.1.0
        with:
          auth: ${{ secrets.GIST_SECRET }}
          gistID: 4a5aafa6640cf68e738697dcc693810d
          filename: vpy_badge_pyhexver.json
          label: Python
          message: ${{ env.PY_HEX_VERSION }}
          color: blue
